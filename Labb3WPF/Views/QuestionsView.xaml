<UserControl x:Class="Labb3WPF.Views.QuestionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Labb3WPF.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="7*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <ListView
            Name="QuestionsLv"
            ItemsSource="{Binding Questions}"
            SelectedItem="{Binding SelectedQuestion, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            SelectionChanged="QuestionsLv_OnSelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Id}" Header="Question Id" />
                    <GridViewColumn DisplayMemberBinding="{Binding Description}" Header="Question" />
                    <GridViewColumn Header="Categories">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <ItemsControl ItemsSource="{Binding Categories}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        <ListView
            Grid.Column="3"
            Grid.Row="0"
            Name="CategoriesLv"
            ItemsSource="{Binding Categories}"
            SelectedItem="{Binding SelectedCategory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Category"/>
                </GridView>
            </ListView.View>
        </ListView>
        <ListView
            Grid.Column="2"
            Grid.Row="0"
            Name="CategoriesInQuestionLv"
            ItemsSource="{Binding CategoriesInQuestion}"
            SelectedItem="{Binding SelectedCategoriesInQuestion, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Category"/>
                </GridView>
            </ListView.View>
        </ListView>
        <ListView
            Grid.Column="1"
            Grid.Row="0"
            Name="AnswersLv"
            ItemsSource="{Binding Answers}"
            SelectedItem="{Binding SelectedAnswer, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Text}" Header="Answer"/>
                    <GridViewColumn DisplayMemberBinding="{Binding IsCorrect}" Header="Correct"/>
                </GridView>
            </ListView.View>
        </ListView>
        <Button
            Grid.Column="3"
            Grid.Row="1"
            Name="AddCategoryToQuestionBtn"
            Content="Add to question"
            Click="AddCategoryToQuestionBtn_OnClick"></Button>
        <Button
            Grid.Column="2"
            Grid.Row="1"
            Name="RemoveCategoryFromQuestionBtn"
            Content="Remove from question"
            Click="RemoveCategoryFromQuestionBtn_OnClick"></Button>
        <Button
            Grid.Column="1"
            Grid.Row="1"
            Name="ResetFilterBtn"
            Content="Reset Filter"
            Click="ResetFilterBtn_OnClick"></Button>
        <ComboBox
            Grid.Column="0"
            Grid.Row="1"
            Name="CategoriesBox"
            ItemsSource="{Binding Categories}"
            SelectedItem="{Binding SelectedCategoryInFilter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            DisplayMemberPath="Name"
            SelectionChanged="CategoriesBox_OnSelectionChanged"/>

    </Grid>
</UserControl>